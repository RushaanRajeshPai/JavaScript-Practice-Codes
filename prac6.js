//let div = document.querySelector("div");
//console.dir(div);

//console.dir(div.innerHTML);

//console.dir(div.innerText);
//above topics are theory, below are its practical uses

let divs = document.querySelectorAll(".box");
divs[0].innerText = "Box 1 changed to Box A";
divs[2].innerText = "Box 3 changed to Box C";